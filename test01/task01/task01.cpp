#include <iostream>

using namespace std;

int main()
{
	//a Covid-33

	/*double temperature;
	bool dry_cough, pain_in_throat, smell, pcr;
	
	cin >> dry_cough >> temperature >> pain_in_throat >> smell >> pcr;

	bool covid_33 = (dry_cough && temperature >= 37.5 && temperature <= 40.5 && pain_in_throat) || (dry_cough && !smell) || pcr;
	
	covid_33 ? cout << "true" : cout << "false";*/

	
	
	//b number

	/*int number;
	cin >> number;

	bool valid_number = number / 1000 == 0;

	bool digits = (number % 10 != number / 10 % 10) && (number / 10 % 10 != number / 100) && (number != number / 100);
	
	valid_number && digits ? cout << "true" : cout << "false";*/


	return 0;
}
